import { NextRequest, NextResponse } from 'next/server'; import { requireCostStructureAdmin } from '@/lib/cost-structure/auth'; import { createMaterialitySuccessor, type MaterialitySuccessorInput } from '@/lib/cost-fluctuation/materiality/rules'; import { jsonBody, positiveSafeInteger } from '@/lib/cost-fluctuation/validation';
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const auth=await requireCostStructureAdmin(request);if('error'in auth)return auth.error;try{const body=await jsonBody(request);return NextResponse.json(await createMaterialitySuccessor(positiveSafeInteger((await params).id,'ruleId'),body as unknown as MaterialitySuccessorInput,positiveSafeInteger(auth.user.uid,'userId')),{status:201});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Successor creation failed.'},{status:400});}}
