import { NextRequest, NextResponse } from 'next/server'; import { requireCostStructureReview } from '@/lib/cost-structure/auth'; import { returnCommentary } from '@/lib/cost-fluctuation/commentary/service'; import { jsonBody, positiveSafeInteger } from '@/lib/cost-fluctuation/validation';
export async function POST(request: NextRequest, { params }: { params: Promise<{id:string}> }) { const auth=await requireCostStructureReview(request); if('error'in auth)return auth.error; try{const body=await jsonBody(request);return NextResponse.json({commentary:await returnCommentary(positiveSafeInteger((await params).id,'commentaryId'),positiveSafeInteger(auth.user.uid,'userId'),body.reviewerNote)});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Return failed.'},{status:400});} }
