import { NextRequest, NextResponse } from 'next/server'; import { requireCostStructureReview } from '@/lib/cost-structure/auth'; import { completePeriodReview } from '@/lib/cost-fluctuation/review/service'; import { jsonBody, positiveSafeInteger } from '@/lib/cost-fluctuation/validation';
export async function POST(request:NextRequest){const auth=await requireCostStructureReview(request);if('error'in auth)return auth.error;try{const body=await jsonBody(request);return NextResponse.json({review:await completePeriodReview(positiveSafeInteger(body.periodId,'periodId'),positiveSafeInteger(auth.user.uid,'userId'),body.note)});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Completion failed.'},{status:400});}}
