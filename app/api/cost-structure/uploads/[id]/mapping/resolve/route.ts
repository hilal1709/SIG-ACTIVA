import {NextRequest,NextResponse} from 'next/server';import {requireCostStructurePrepare} from '@/lib/cost-structure/auth';import {resolveSourceMapping} from '@/lib/cost-structure/mappings/resolution-service';
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const auth=await requireCostStructurePrepare(request);if('error'in auth)return auth.error;try{const body=await request.json();const result=await resolveSourceMapping({...body,uploadId:Number((await params).id)},Number(auth.user.uid));return NextResponse.json(result)}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Resolusi gagal.'},{status:400})}}
