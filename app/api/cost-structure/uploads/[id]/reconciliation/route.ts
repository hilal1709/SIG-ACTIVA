import {NextRequest,NextResponse} from 'next/server'; import {requireCostStructureRead} from '@/lib/cost-structure/auth'; import {getPhaseDReport} from '@/lib/cost-structure/reconciliation/service';
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){const auth=await requireCostStructureRead(request);if('error'in auth)return auth.error;const report=await getPhaseDReport(Number((await params).id));if(!report)return NextResponse.json({error:'Upload tidak ditemukan.'},{status:404});return NextResponse.json({sources:report.sources,completeness:report.completeness,blockers:report.blockers,ready:report.ready,periodStatus:report.upload.period.status});}
